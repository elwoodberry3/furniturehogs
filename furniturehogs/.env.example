# n8n webhook endpoints (Baseline Stack orchestration layer)
# When unset, API routes fall back to demo-mode — the app runs fully clickable with zero credentials.
N8N_AVAILABILITY_URL=
N8N_BOOKING_URL=

# Optional shared secret to sign requests to n8n (recommended before production)
N8N_WEBHOOK_SECRET=
